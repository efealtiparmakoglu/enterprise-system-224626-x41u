apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-224626-x41u-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-224626-x41u.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-224626-x41u-service
            port:
              number: 80
